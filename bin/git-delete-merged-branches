#!/usr/bin/env bash

branches=$(git branch --no-color --merged | grep -v "\*" | grep -vE "\b(master|dev|develop)\b" | grep -v svn)
if [ -n "$branches" ]
then
    echo "$branches" | xargs -rn 1 git branch -d
fi
