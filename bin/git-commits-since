#!/usr/bin/env bash

#@@ common: @@
#@@ include: helper/is-git-repo.sh @@

SINCE="last week"
test $# -ne 0 && SINCE=$@
echo "... commits since $SINCE" >&2
git log --pretty='%an - %s' --after="@{$SINCE}"