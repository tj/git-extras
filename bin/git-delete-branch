#!/bin/sh

branch=$1
test -z $branch && echo "branch required." 1>&2 && exit 1
git branch -D $branch

# check other repos for the branch
for other in `git branch -r | grep $branch`; do
  repo=${other%/$branch}
  
  git branch -d -r $repo/$branch
  git push $repo :refs/heads/$branch
done

