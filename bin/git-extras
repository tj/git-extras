#!/usr/bin/env bash

VERSION="4.1.0-dev"
# Note: git-extras is special-cased to include the library code for update(). See
# build_commands for details.

case "$1" in
  -v|--version)
    echo $VERSION && exit 0
    ;;
  update)
    update
    ;;
  *)
    git extras --help
    ;;
esac
